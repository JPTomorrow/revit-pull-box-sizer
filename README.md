# Revit-Pull-Box-Sizer
A Revit application that sizes pull boxes based on the number of incoming pipes.

Requires an install of Revit and uses MSBuild.exe to compile. This is a .NET framework application.
